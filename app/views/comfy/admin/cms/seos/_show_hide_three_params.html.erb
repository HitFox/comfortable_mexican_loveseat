<div class= 'show_hide_urls'>
  <% if result.empty? %>
    <div> No data! </div>
  <% else
    result.each do |url, parent, code| %>
      <div> <%= "#{code}: #{url} [parent-> #{parent}]" %> </div>
    <% end %>
  <% end %>
</div>
